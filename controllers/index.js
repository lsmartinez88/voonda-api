/**
 * Exportación centralizada de todos los controllers
 * Usando sintaxis de exports directa para mayor simplicidad
 */

module.exports = {
  authController: require('./authController'),
  vehiculosController: require('./vehiculosController')
};